apiVersion: v1
kind: Pod
metadata:
  name: pod-eth0
  namespace: multus2
spec:
  containers:
    - name: httpd
      image: 'image-registry.openshift-image-registry.svc:5000/openshift/cli:latest'
      command:
        - sh
        - '-c'
        - tail -f /dev/null
